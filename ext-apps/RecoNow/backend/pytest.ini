[pytest]
asyncio_mode = auto

# mutmut copies the whole tree into ./mutants to mutate it; pytest would
# otherwise collect every test twice and fail on the duplicate module names.
norecursedirs = mutants .venv
