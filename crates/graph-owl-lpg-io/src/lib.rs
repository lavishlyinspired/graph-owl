//! Property-graph interchange: GraphML, CSV bulk, Cypher script; sync to external LPG stores.
//!
//! **Status**: placeholder. Implemented by Epic 9a — see `plans/09a-lpg-interchange.md`.
//!
//! Symmetric with `graph-owl-rdf-io`, which does the same job for the RDF serializations.
//! Per-format and per-target driver features so a deployment compiles only what it uses.
