//! In-memory `Storage` implementation, promoted from the integration-test fake.
//! **Status**: placeholder. Implemented by Epic 37c — see `plans/37c-embeddable.md`.
//! What an embedding consumer links instead of Postgres, and what makes the `Storage` port
//! provably a port rather than a Postgres-shaped interface with one implementation.
